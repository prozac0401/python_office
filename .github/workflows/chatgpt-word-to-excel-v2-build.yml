name: Build WordToExcel v2 Windows EXE

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          architecture: 'x64'

      - name: Install build dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install pywin32 pyinstaller

      - name: Syntax check
        shell: pwsh
        run: |
          python -m py_compile _chatgpt_build_v2/word_to_excel.py

      - name: Build single-file GUI executable
        shell: pwsh
        run: |
          python -m PyInstaller `
            --noconfirm `
            --clean `
            --onefile `
            --windowed `
            --name WordToExcel_v2 `
            --hidden-import=pythoncom `
            --hidden-import=pywintypes `
            --hidden-import=win32timezone `
            --collect-submodules=win32com `
            _chatgpt_build_v2/word_to_excel.py
          if (-not (Test-Path 'dist/WordToExcel_v2.exe')) { throw 'WordToExcel_v2.exe was not created.' }
          Get-Item 'dist/WordToExcel_v2.exe' | Format-List Name,Length,LastWriteTime

      - name: Upload executable package
        uses: actions/upload-artifact@v4
        with:
          name: WordToExcel_v2_Windows
          if-no-files-found: error
          retention-days: 7
          path: |
            dist/WordToExcel_v2.exe
            _chatgpt_build_v2/README.txt
